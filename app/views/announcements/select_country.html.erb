<h2><%= t('select_country', :default => "Välj land") %>:</h2>

<%= simple_form_for @user, url: wizard_path(:accept_terms), :method => "GET" do |f| %>
  <%= country_select("user", "country", [ "#{request.location.try(:country).try(:humanize)}"]) %>
  <%= f.submit t('submit', :default => "Skicka") %>
<% end %>
